import React from 'react';
import PropTypes from 'prop-types';
import styles from './Recursion.module.css';

const Recursion = ({files}) => (
  <div className={styles.Recursion}>
    Recursion Component
  </div>
);

Recursion.propTypes = {};

Recursion.defaultProps = {};

export default Recursion;
